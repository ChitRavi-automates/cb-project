apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: chitra-doe24-test
  namespace: default
spec:
  secretName: chitra-doe24-tls
  issuerRef:
    name: letsencrypt-staging
    kind: ClusterIssuer
  dnsNames:
    - "chitra.doe24.chas-lab.dev"
